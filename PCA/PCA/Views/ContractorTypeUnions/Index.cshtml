@model IEnumerable<PCA.Models.ContractorTypeUnion>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_LayoutAdmin.cshtml";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Contractors.Name)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.ContractorTypes.Name)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Contractors.Name)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.ContractorTypes.Name)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.ContractorTypeUnionId }) |
            @Html.ActionLink("Details", "Details", new { id=item.ContractorTypeUnionId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.ContractorTypeUnionId })
        </td>
    </tr>
}

</table>
